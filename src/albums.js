const albumsBlurb = {
    fontWeight: 'bold',
    fontSize: '48px',
    display: 'flex',
    padding: '10px'
}

export default function Albums(){

    return(
        <div style={albumsBlurb}>
            Listened to a new album?
        </div>
    )
}